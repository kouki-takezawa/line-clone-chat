# 容量・課金安全設計 ＆ セキュリティ設計仕様書

100人規模での無料運用を前提に、「絶対に課金されない」「容量オーバーで壊れない」「情報が漏洩しない」を目標に設計する。実装前のレビュー用ドキュメント。

---

## 1. 課金されないことの保証（最重要・大前提）

**Supabase Free / Vercel Hobbyは、支払い方法を登録しない限り自動的に課金されることはない。** 無料枠を超えた場合の挙動は「課金」ではなく「機能制限・拒否」（例: 新規書き込み拒否、リクエストのレート制限）。

- **Supabase**: プロジェクトに支払い方法（クレジットカード）を一切登録しない。これにより物理的に課金が発生し得ない状態にする。
- **Vercel**: Hobbyプランのまま、Proへのアップグレードやカード登録をしない。

→ 以下の容量対策は「無料枠内で気持ちよく動き続けるための安全策」であり、「課金を防ぐための対策」ではない（そもそも課金され得ない）。目的は**サービス停止・データ破損を防ぐこと**。

## 2. 無料枠の上限（現状値）

| リソース | 無料枠上限 | 今回の設計での想定 |
|---|---|---|
| Supabase DB容量 | 500MB | 24hで消えるテキストのみ。100人でも数MB程度 |
| Supabase Storage容量 | 1GB | 画像。**要対策**（下記3.1） |
| Supabase 帯域(Egress) | 5GB/月 | 画像配信。圧縮前提で余裕あり |
| Supabase Realtime同時接続 | 200 | 開いているトーク画面の数だけ消費。100人が同時オンラインでも1人1接続なら余裕 |
| Vercel帯域 | 100GB/月 | 静的アセット中心、余裕あり |
| Vercelサーバーレス関数呼び出し | 月100万回 | push通知配信などで使用、余裕あり |

**懸念点はStorage容量（画像）の1点に集約される。** 24時間で消えるとはいえ、100人が同時にアクティブだと「常に1日分」の画像が溜まった状態になり得るため、ここだけ具体的な上限を設ける。

## 3. 具体的な安全策（実装予定）

### 3.1 画像アップロードの上限（Storage容量対策）

- 圧縮後の目標サイズ: **200KB以下**（現状の`compressImage()`の設定を調整）
- 1人・24時間あたりの画像送信上限: **20枚まで**（超えたらエラー表示、翌日リセット）
  - ワーストケース試算: 100人 × 20枚 × 200KB = **約400MB**（1GB枠に対して十分な余裕を残す）
- アップロード前にファイルサイズを検証し、極端に大きい元ファイル（例: 20MB超）は圧縮前に拒否

### 3.2 メッセージ本文の文字数制限（DB容量対策・既に計画済み）

- 1通あたり最大1,000文字（クライアント側`maxLength`＋DB側`check`制約の二重）

### 3.3 登録ユーザー数の上限

- サインアップ時に現在の登録者数をチェックし、**上限110人**（想定100人＋バッファ）を超えたら新規登録を拒否するメッセージを表示
- 悪意・誤操作による無限増殖を防ぐハードリミット

### 3.4 友達申請のスパム防止

- 1人が同時に保持できる「送信済み・未承認」の友達申請数の上限（例: 20件）
- 同一相手への再送信は前回が拒否/承認されるまで不可

### 3.5 Realtime接続の節約

- 現在開いているトーク画面の分だけSupabase Realtimeチャンネルを購読し、それ以外（トーク一覧・友達一覧など）では購読しない設計を維持
- 「誰がオンラインか」のようなプレゼンス機能は今回スコープ外とする（実装すると全ユーザーが常時接続を維持するようになり、200接続枠を圧迫するため）

### 3.6 監視・可観測性

- 自動アラートの仕組みまでは無料枠内では構築が難しいため、**Supabase Dashboard → Usageタブを月1回程度目視確認する運用を推奨**（具体的な確認手順はSETUP.mdに追記する）

---

## 4. セキュリティ設計

### 4.1a セッション管理（localStorageではなくCookie）

現行の実装（`@supabase/ssr`）は、もともとlocalStorageではなく**Cookieベース**でセッションを保持している（採用時からの設計）。確認した実際の設定値は以下の通り:

- `sameSite: lax`（設定済み・CSRF対策として機能）
- `httpOnly: false`（ライブラリのデフォルト仕様）

**`httpOnly`をtrueにできない理由**: このアプリはブラウザから直接Supabaseへ話しかける構成（Realtimeのリアルタイム購読、画像アップロード等をブラウザのJSが直接実行）を取っており、これはSupabaseクライアントSDKの標準的な使い方。ブラウザ側JSがトークンを読めない完全HttpOnly構成にするには、**全てのSupabase通信を自前のAPI Routeでラップし直す**大改修が必要（Realtimeの低遅延な直接WebSocket接続という利点も失う）。100人規模のコミュニティアプリという想定脅威レベルに対して費用対効果が見合わないため、**現行のCookie+SameSite=Lax構成を維持することを推奨**する。XSS自体への対策（Reactの自動エスケープ徹底、`dangerouslySetInnerHTML`不使用）でトークン窃取の入口自体を塞ぐ方針とする。

### 4.1 認証・アカウント保護

- メールアドレスによる本人確認必須（確認前はログイン不可）
- パスワード最小長を6→**8文字以上**に引き上げ（Supabase Auth設定、クライアント側でも同様に強制）
- ログイン試行のブルートフォース対策はSupabase Auth標準のレート制限に依拠（追加実装不要）
- パスワードリセットはメールリンク経由のみ。トークンは短時間で失効（Supabase標準仕様）

### 4.2 認可・アクセス制御（RLSを唯一の実質的な境界とする）

- 全テーブルでRow Level Security (RLS) を有効化し、「自分が当事者のデータのみ」参照・更新できることを徹底する。UI側の非表示だけに頼らない
- **ブロック機能**: UIで非表示にするだけでなく、RLSレベルでブロック関係にあるユーザー間の友達申請送信・メッセージ送信を拒否する（ブロック解除まで技術的に不可能にする）
- サービスロールキー（RLSを無視できる強力な鍵）はサーバー環境変数にのみ保持し、`server-only`パッケージでクライアントバンドルへの混入をビルドエラーとして検知する仕組みを維持

### 4.3 データそのものの保護

- 画像はStorageの**非公開バケット**に保存。直リンク不可、**期限付き署名URL（5分）**でのみ配信し、URLが流出・使い回しされても短時間で無効化される（当初案の1時間から短縮。トーク画面表示のたびに再取得する設計のため、5分でも体験上の支障はない）
- メッセージ・画像は送信から24時間で**物理削除**（DBの行・Storageのファイル実体の両方）。これまで通りログを一切残さない設計
- 既読情報も個別のログテーブルを持たず、「最終既読時刻」1つの値の比較のみで実現（消し忘れるデータを構造的に作らない）
- 通信は全経路HTTPS（Vercel・Supabaseとも標準で強制、変更不要）

### 4.4 入力値の検証・インジェクション対策

- メッセージ本文はReactの標準エスケープで表示するため、保存されたXSSは発生しない（`dangerouslySetInnerHTML`は使用しない方針を継続）
- 全てのDB操作はSupabase公式クライアント経由のパラメータ化クエリのみを使用し、生SQLの組み立ては行わない（SQLインジェクション対象外）
- 文字数・画像サイズの制限をクライアント側とDB制約の二重で強制し、APIを直接叩く形の迂回にも対応

### 4.5 シークレット管理

- service role key・VAPID秘密鍵はVercel環境変数（サーバー専用、`NEXT_PUBLIC_`接頭辞なし）にのみ保持
- 24時間削除処理が使うservice role keyはSupabase Vaultに暗号化保存（既に対応済み）
- いずれの秘密情報もgitリポジトリには一切コミットしない（`.env.local`は`.gitignore`済み、確認テンプレートのみコミット）

### 4.7 レートリミット（連投・スクリプト荒らし対策）

クライアント側の制御（ボタン連打防止など）だけでは、APIを直接叩くスクリプトによる連投を防げない。**DBのトリガーとして強制**し、クライアントを経由しない攻撃にも対応する:

- 同一送信者が**1秒間に3通以上**メッセージを送ろうとした場合、DBトリガーが`INSERT`を拒否（エラーを返す）
- 同一送信者が**1分間に5枚以上**の画像を送ろうとした場合も同様に拒否
- Postgresの`BEFORE INSERT`トリガーで直近の送信件数を数えて判定するため、追加のインフラ（Redis等）は不要

### 4.6 意図的にスコープ外とする項目（正直な明記）

- **コンテンツモデレーション**: 不適切な画像・迷惑行為の自動検知は実装しない。ユーザーができる唯一の対処は「ブロック」
- **高度なボット対策**: IPレート制限・CAPTCHA等は実装せず、Supabase Auth標準のレート制限のみに依拠
- **監査ログ**: 「誰が何をしたか」の記録は意図的に持たない（本アプリの「ログを残さない」というコンセプト自体に反するため）

---

## 5. まとめ（判断していただきたいポイント）

1. 画像は**200KB圧縮・1人1日20枚まで**という具体的な上限でよいか（変更したい場合は数値を指定してください）
2. 登録者数の上限を**110人**としてよいか
3. パスワード最小長を**8文字以上**に引き上げてよいか
4. コンテンツモデレーション・高度なボット対策・監査ログは**スコープ外のままでよいか**（将来的に必要なら別途追加実装）

上記でよければ、この仕様をフェーズ1〜4の実装に組み込んで進めます。
